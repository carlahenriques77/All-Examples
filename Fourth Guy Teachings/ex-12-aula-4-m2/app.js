let i = 0
while (i < 5) {
    console.log(`Dentro do loop: ${i}`);
    i++ // The Whole "while" Loop thing. At first, I thought that it was better than "for Loop", but it seems like this one here isn't, as it has a high risk of doing the "Infinite Loop" that crashed our Computer that time.
}

const names = ['Jobs', 'Hopper', 'Turing']
let p = 0

while (p < names.length) {
    console.log(names[i]);
    i++
}